deployments:
  - include: stage1
  - barrier: true
  - include: stage2
  - barrier: true
  - include: stage3
  - barrier: true
  - include: stage4

commonLabels:
  k-ship.target: {{ target.name }}
  k-ship.environment: {{ args.environment }}
